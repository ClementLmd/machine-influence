export enum UserRole {
  RECRUITER = 'RECRUITER',
  INDEPENDENT = 'INDEPENDENT',
}

export enum AnnonceStatus {
  ACTIVE = 'ACTIVE',
  CLOSED = 'CLOSED',
  DRAFT = 'DRAFT',
}

export enum CandidatureStatus {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  REJECTED = 'REJECTED',
}
